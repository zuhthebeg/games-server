name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare dist (gitignored; functions bundle from root)
        run: mkdir -p dist && [ -f dist/index.html ] || printf '<!doctype html><meta charset="utf-8"><title>games-relay</title>' > dist/index.html

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: 3d0681b782422e56226a0a1df4a0e8b2
          command: pages deploy ./dist --project-name=games-relay --commit-dirty=true --commit-message="ci deploy"
